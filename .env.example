# Copy to .env (gitignored) and fill in. NEVER commit real keys.
OPENAI_API_KEY=sk-your-key-here
GROQ_API_KEY=gsk-your-key-here   # only if used
# Optional: comma-separated allowed origins for the AI proxy (e.g. https://yoursite.netlify.app)
ALLOWED_ORIGINS=
